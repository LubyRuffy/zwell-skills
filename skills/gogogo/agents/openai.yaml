interface:
  display_name: "GoGoGo"
  short_description: "把后续请求切到从 issue 到上线的完整闭环执行模式"
  default_prompt: "Use $gogogo to treat the following request as a full lifecycle workflow: if the user already provides an issue number or issue URL, skip issue creation; otherwise create a tracking issue first, then continue through implementation, verification, review, integration, deployment, and closure."
